# Optional override for the backend origin.
# Leave empty to use same-origin relative requests (default), which work with
# the Vite dev proxy locally and the nginx proxy under docker-compose.
VITE_API_BASE_URL=
